# Agent Instructions

## Static HTML Constraint
- The user has exported the site to a pure static HTML format. 
- **CRITICAL ROLE**: From now on, any changes made to the website MUST be made directly to `index.html` (or other static `.html`, `.css`, or `.js` files) using vanilla HTML/JS/CSS.
- **DO NOT** modify `src/`, React components, or Vite configurations.
- **DO NOT** use build steps or npm commands unless explicitly requested to serve the directory. The site is now a pure vanilla web page.
